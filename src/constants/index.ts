import { colors } from './colors';
import { dimensions } from './dimensions';

export { colors };
export { colors as Colors } from './colors';
export { dimensions };
export { dimensions as Dimensions } from './dimensions';
export { Routes } from './routes';

export const Spacing = dimensions.spacing;
export const BorderRadius = dimensions.borderRadius;
export const FontSize = dimensions.fontSize;
