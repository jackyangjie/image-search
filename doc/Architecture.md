# AI 相册搜索 APP - 架构设计文档

## 文档信息

| 属性 | 值 |
|------|-----|
| 版本 | v1.0 |
| 最后更新 | 2026-04-04 |
| 作者 | AI助手 |

---

## 目录

1. [系统架构](#1-系统架构)
2. [数据流设计](#2-数据流设计)
3. [时序图](#3-时序图)
4. [模块依赖关系](#4-模块依赖关系)
5. [状态机设计](#5-状态机设计)
6. [错误处理流程](#6-错误处理流程)

---

## 1. 系统架构

### 1.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Application Layer                               │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────────────────┐  │
│  │   SearchScreen  │  │  GalleryScreen  │  │      PhotoDetailScreen      │  │
│  │                 │  │                 │  │                             │  │
│  │  ┌───────────┐  │  │  ┌───────────┐  │  │  ┌───────────────────────┐  │  │
│  │  │ SearchBar │  │  │  │ PhotoGrid │  │  │  │    PhotoViewer        │  │  │
│  │  └───────────┘  │  │  └───────────┘  │  │  └───────────────────────┘  │  │
│  │  ┌───────────┐  │  │  ┌───────────┐  │  │  ┌───────────────────────┐  │  │
│  │  │ResultGrid │  │  │  │ScanButton │  │  │  │    ActionButtons      │  │  │
│  │  └───────────┘  │  │  └───────────┘  │  │  └───────────────────────┘  │  │
│  └────────┬────────┘  └────────┬────────┘  └──────────────┬────────────────┘  │
│           │                    │                         │                   │
└───────────┼────────────────────┼─────────────────────────┼───────────────────┘
            │                    │                         │
            └────────────────────┼─────────────────────────┘
                                 │
┌────────────────────────────────┼─────────────────────────────────────────────┐
│                          Services Layer                                      │
│  ┌──────────────────┐  ┌─────┴──────┐  ┌──────────────────────────────────┐ │
│  │   SearchService  │  │  AppState  │  │        ScannerService            │ │
│  │                  │  │   (Context)│  │                                  │ │
│  │ ┌──────────────┐ │  │            │  │  ┌──────────┐    ┌──────────┐   │ │
│  │ │AIService     │ │  │ - photos   │  │  │  Batch   │───▶│ Thumbnail│   │ │
│  │ └──────────────┘ │  │ - results  │  │  │ Processor│    │ Generator│   │ │
│  │ ┌──────────────┐ │  │ - scanState│  │  └──────────┘    └──────────┘   │ │
│  │ │DBService     │ │  │            │  │                                  │ │
│  │ └──────────────┘ │  └────────────┘  └──────────────────────────────────┘ │
│  └──────────────────┘                                                        │
└──────────────────────────────────────────────────────────────────────────────┘
                                 │
┌────────────────────────────────┼─────────────────────────────────────────────┐
│                      Native Modules Layer                                    │
│  ┌──────────────────┐  ┌───────┴────┐  ┌──────────────────────────────────┐ │
│  │  ONNX Runtime    │  │   SQLite   │  │      Media Library               │ │
│  │                  │  │            │  │                                  │ │
│  │ ┌──────────────┐ │  │ ┌────────┐ │  │  ┌──────────┐    ┌──────────┐   │ │
│  │ │MobileCLIP    │ │  │ │Vector  │ │  │  │  Photo   │    │  Album   │   │ │
│  │ │  - Text      │ │  │ │Storage │ │  │  │  Access  │    │  Access  │   │ │
│  │ │  - Vision    │ │  │ └────────┘ │  │  └──────────┘    └──────────┘   │ │
│  │ └──────────────┘ │  └────────────┘  └──────────────────────────────────┘ │
│  └──────────────────┘                                                        │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 分层说明

| 层级 | 职责 | 主要组件 |
|------|------|---------|
| Application | UI 渲染、用户交互 | Screens, Components |
| Services | 业务逻辑、状态管理 | Services, Context |
| Native | 原生能力调用 | ONNX, SQLite, Media |

---

## 2. 数据流设计

### 2.1 搜索流程数据流

```
用户输入搜索词
      │
      ▼
┌─────────────┐
│ SearchScreen│
└──────┬──────┘
       │ 1. 调用 search(query)
       ▼
┌──────────────┐
│SearchService │
└──────┬───────┘
       │ 2. 文本向量化
       ▼
┌─────────────┐
│  AIService  │
│ encodeText()│
└──────┬──────┘
       │ 3. 返回 512维向量
       ▼
┌──────────────┐
│SearchService │
└──────┬───────┘
       │ 4. 向量搜索
       ▼
┌─────────────┐
│  DBService  │
│searchByVector│
└──────┬──────┘
       │ 5. 查询所有照片向量
       ▼
┌─────────────┐
│   SQLite    │
└──────┬──────┘
       │ 6. 返回照片列表
       ▼
┌──────────────┐     ┌─────────────┐
│SearchService │────▶│  计算相似度  │
└──────┬───────┘     └─────────────┘
       │ 7. 排序返回结果
       ▼
┌─────────────┐
│ SearchScreen│
│ 更新 UI     │
└─────────────┘
```

### 2.2 扫描流程数据流

```
用户点击"扫描相册"
      │
      ▼
┌─────────────┐
│GalleryScreen│
└──────┬──────┘
       │ 1. 请求权限
       ▼
┌────────────────┐
│PermissionService│
└────────┬───────┘
         │ 2. 权限 granted
         ▼
┌─────────────┐
│ScannerService│
│  startScan() │
└──────┬──────┘
       │ 3. 获取照片列表
       ▼
┌────────────────┐
│  Media Library │
└────────┬───────┘
         │ 4. 返回照片 URI 列表
         ▼
┌─────────────┐
│ScannerService│
└──────┬──────┘
       │ 5. 循环处理每张照片
       ▼
┌─────────────────────────────────────────┐
│           Batch Processor               │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │ Photo 1 │  │ Photo 2 │  │ Photo N │ │
│  └───┬─────┘  └────┬────┘  └────┬────┘ │
│      │             │            │      │
│      ▼             ▼            ▼      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │Generate │  │Generate │  │Generate │ │
│  │Thumb    │  │Thumb    │  │Thumb    │ │
│  └────┬────┘  └────┬────┘  └────┬────┘ │
│       │            │            │      │
│       ▼            ▼            ▼      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │  AI     │  │  AI     │  │  AI     │ │
│  │Encode   │  │Encode   │  │Encode   │ │
│  └────┬────┘  └────┬────┘  └────┬────┘ │
│       │            │            │      │
│       ▼            ▼            ▼      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │ Save to │  │ Save to │  │ Save to │ │
│  │   DB    │  │   DB    │  │   DB    │ │
│  └─────────┘  └─────────┘  └─────────┘ │
└─────────────────────────────────────────┘
       │ 6. 批量写入数据库
       ▼
┌─────────────┐
│   DBService │
└──────┬──────┘
       │ 7. 更新完成
       ▼
┌─────────────┐
│GalleryScreen│
│ 刷新列表    │
└─────────────┘
```

### 2.3 状态管理数据流

```
┌─────────────────────────────────────────────────────────────┐
│                      React Context                           │
│                    (AppStateProvider)                        │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                       State                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐ │   │
│  │  │   photos    │  │ searchResults│  │  scanState   │ │   │
│  │  │  (Photo[])  │  │ (SearchResult[]│  │(ScanState)  │ │   │
│  │  └─────────────┘  └─────────────┘  └──────────────┘ │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐ │   │
│  │  │searchQuery  │  │ isScanning  │  │ scanProgress │ │   │
│  │  │  (string)   │  │  (boolean)  │  │(ScanProgress)│ │   │
│  │  └─────────────┘  └─────────────┘  └──────────────┘ │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │                     Actions                          │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐ │   │
│  │  │ scanPhotos  │  │searchPhotos │  │deletePhoto   │ │   │
│  │  │  (async)    │  │  (async)    │  │  (async)     │ │   │
│  │  └─────────────┘  └─────────────┘  └──────────────┘ │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐ │   │
│  │  │refreshGallery│  │  setQuery   │  │   clearSearch│ │   │
│  │  │  (async)    │  │  (sync)     │  │   (sync)     │ │   │
│  │  └─────────────┘  └─────────────┘  └──────────────┘ │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
         │            │            │
         ▼            ▼            ▼
   ┌──────────┐ ┌──────────┐ ┌──────────┐
   │ Services │ │ Services │ │ Services │
   │  Layer   │ │  Layer   │ │  Layer   │
   └──────────┘ └──────────┘ └──────────┘
```

---

## 3. 时序图

### 3.1 应用启动时序

```sequence
User->>App: 启动应用
App->>AppStateProvider: 初始化 Context
AppStateProvider->>DBService: initialize()
DBService->>SQLite: 打开数据库
SQLite-->>DBService: 连接成功
DBService-->>AppStateProvider: 初始化完成
AppStateProvider->>AIService: initialize()
AIService->>ONNX: 加载 MobileCLIP 模型
ONNX-->>AIService: 模型加载完成
AIService-->>AppStateProvider: 初始化完成
AppStateProvider->>DBService: getPhotos({limit: 50})
DBService->>SQLite: SELECT * FROM photos
SQLite-->>DBService: 返回照片列表
DBService-->>AppStateProvider: photos[]
AppStateProvider-->>App: 状态就绪
App->>SearchScreen: 渲染首页
```

### 3.2 自然语言搜索时序

```sequence
User->>SearchScreen: 输入"海边的日落"
SearchScreen->>SearchService: search("海边的日落")
SearchService->>SearchService: addToHistory()
SearchService->>AIService: encodeText("海边的日落")
AIService->>MobileCLIP: 文本编码
MobileCLIP-->>AIService: text embedding[512]
AIService-->>SearchService: embedding[]
SearchService->>DBService: searchByVector(embedding, 20)
DBService->>SQLite: SELECT * FROM photos WHERE isIndexed=1
SQLite-->>DBService: 所有照片向量
DBService->>DBService: 计算余弦相似度
DBService->>DBService: 按相似度排序
DBService->>DBService: 取 Top-20
DBService-->>SearchService: SearchResult[]
SearchService-->>SearchScreen: 返回结果
SearchScreen->>SearchScreen: 更新 ResultGrid
SearchScreen-->>User: 显示搜索结果
```

### 3.3 相册扫描时序

```sequence
User->>GalleryScreen: 点击"扫描相册"
GalleryScreen->>PermissionService: checkPhotoPermission()
PermissionService-->>GalleryScreen: status: 'notDetermined'
GalleryScreen->>PermissionService: requestPhotoPermission()
PermissionService->>MediaLibrary: 申请权限
MediaLibrary-->>PermissionService: granted
PermissionService-->>GalleryScreen: granted
GalleryScreen->>ScannerService: startScan()
ScannerService->>ScannerService: 更新状态: scanning
ScannerService->>EventBus: emit('scan:start')
ScannerService->>MediaLibrary: getAssetsAsync()
MediaLibrary-->>ScannerService: Asset[]
ScannerService->>DBService: getPhotosCount()
DBService-->>ScannerService: count
ScannerService->>ScannerService: 过滤已索引照片
loop 每张照片
    ScannerService->>ThumbnailGenerator: generate(uri)
    ThumbnailGenerator-->>ScannerService: thumbnailPath
    ScannerService->>AIService: encodeImage(uri)
    AIService->>MobileCLIP: 图片编码
    MobileCLIP-->>AIService: image embedding[512]
    AIService-->>ScannerService: embedding[]
    ScannerService->>DBService: insertPhoto()
    DBService->>SQLite: INSERT
    SQLite-->>DBService: success
    DBService-->>ScannerService: photoId
    ScannerService->>EventBus: emit('scan:progress')
end
ScannerService->>ScannerService: 更新状态: completed
ScannerService->>EventBus: emit('scan:complete')
ScannerService-->>GalleryScreen: ScanResult
GalleryScreen->>DBService: getPhotos({limit: 50})
DBService-->>GalleryScreen: photos[]
GalleryScreen->>GalleryScreen: 刷新 PhotoGrid
```

### 3.4 查看照片详情时序

```sequence
User->>PhotoGrid: 点击照片
PhotoGrid->>Navigation: navigate('PhotoDetail', {photoId})
Navigation->>PhotoDetailScreen: 渲染
PhotoDetailScreen->>PhotoService: getPhotoDetail(photoId)
PhotoService->>DBService: getPhoto(photoId)
DBService->>SQLite: SELECT * FROM photos WHERE id=?
SQLite-->>DBService: Photo
DBService-->>PhotoService: Photo
PhotoService->>PhotoService: 读取 EXIF 信息
PhotoService->>PhotoService: 格式化数据
PhotoService-->>PhotoDetailScreen: PhotoDetail
PhotoDetailScreen->>PhotoDetailScreen: 渲染大图
User->>PhotoDetailScreen: 点击"相似图片"
PhotoDetailScreen->>PhotoService: findSimilarPhotos(photoId, 20)
PhotoService->>DBService: getPhoto(photoId)
DBService-->>PhotoService: Photo with embedding
PhotoService->>DBService: searchByVector(embedding, 20)
DBService-->>PhotoService: SearchResult[]
PhotoService-->>PhotoDetailScreen: Photo[]
PhotoDetailScreen->>PhotoDetailScreen: 显示相似图片网格
```

---

## 4. 模块依赖关系

### 4.1 依赖图

```
                    ┌─────────────┐
                    │    App      │
                    └──────┬──────┘
                           │
              ┌────────────┼────────────┐
              │            │            │
              ▼            ▼            ▼
       ┌─────────────┐ ┌────────┐ ┌─────────────┐
       │ SearchScreen│ │Gallery │ │PhotoDetail  │
       └──────┬──────┘ │ Screen │ └─────────────┘
              │        └───┬────┘
              │            │
              └────────────┼────────────┐
                           │            │
                           ▼            ▼
                    ┌─────────────┐ ┌─────────────┐
                    │ SearchService│ │ScannerService│
                    └──────┬──────┘ └──────┬──────┘
                           │               │
              ┌────────────┼────────────┐  │
              │            │            │  │
              ▼            ▼            ▼  │
       ┌─────────────┐ ┌────────┐ ┌────────┴─────┐
       │  AIService  │ │DBService│ │PhotoService │
       └──────┬──────┘ └───┬────┘ └──────┬───────┘
              │            │             │
              ▼            ▼             ▼
       ┌─────────────┐ ┌────────┐ ┌─────────────┐
       │    ONNX     │ │ SQLite │ │MediaLibrary │
       │  Runtime    │ │        │ │             │
       └─────────────┘ └────────┘ └─────────────┘
```

### 4.2 依赖规则

| 模块 | 可以依赖 | 禁止依赖 |
|------|---------|---------|
| Screens | Components, Services, Context | Native Modules |
| Components | Services, Context | Native Modules |
| Services | 其他 Services, Native Modules | Screens, Components |
| Context | Services | Native Modules |
| Native | 无 | 任何上层模块 |

---

## 5. 状态机设计

### 5.1 扫描状态机

```
                    ┌─────────┐
        ┌───────────│  IDLE   │◀────────────┐
        │           │ (初始)   │             │
        │           └────┬────┘             │
        │                │ startScan()      │
        │                ▼                  │
        │           ┌─────────┐             │
        │      ┌────│SCANNING │────┐        │
        │      │    │ (扫描中) │    │        │
        │      │    └────┬────┘    │        │
   cancel()    │         │         │  pause()
        │      │         │         │        │
        │      ▼         │         ▼        │
        │  ┌─────────┐   │    ┌─────────┐   │
        └──│COMPLETED│   │    │ PAUSED  │───┘
           │ (完成)   │   │    │ (暂停)  │
           └─────────┘   │    └─────────┘
                         │ resume()
                         ▼
                    ┌─────────┐
                    │  ERROR  │
                    │ (错误)   │
                    └─────────┘
```

**状态说明**:

| 状态 | 说明 | 允许操作 |
|------|------|---------|
| IDLE | 初始状态，未开始扫描 | startScan() |
| SCANNING | 正在扫描和索引照片 | pause(), cancel() |
| PAUSED | 扫描暂停 | resume(), cancel() |
| COMPLETED | 扫描完成 | startScan() (重新开始) |
| ERROR | 扫描出错 | startScan() (重试) |

### 5.2 搜索状态机

```
┌─────────┐     输入查询词      ┌───────────┐
│  IDLE   │──────────────────▶│ SEARCHING │
│ (空闲)   │                   │ (搜索中)   │
└────┬────┘◀───────────────────┘─────┬─────┘
     │         结果返回/无结果        │
     │                                │
     │         ┌───────────┐          │
     └────────▶│  RESULTS  │◀─────────┘
               │ (有结果)   │
               └─────┬─────┘
                     │ 清空/新搜索
                     ▼
               ┌───────────┐
               │ NO_RESULTS│
               │ (无结果)   │
               └───────────┘
```

---

## 6. 错误处理流程

### 6.1 错误处理架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Error Boundary                         │
│                    (React Error Boundary)                   │
└─────────────────────────────────┬───────────────────────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
            ┌───────────┐  ┌───────────┐  ┌───────────┐
            │ UI Error  │  │Service Err│  │Native Err │
            │ Handler   │  │  Handler  │  │  Handler  │
            └─────┬─────┘  └─────┬─────┘  └─────┬─────┘
                  │              │              │
                  ▼              ▼              ▼
            ┌─────────────────────────────────────────┐
            │            Error Logger                 │
            │       (本地日志 + 可选上报)              │
            └─────────────────────────────────────────┘
```

### 6.2 错误处理流程图

```
操作失败
   │
   ▼
┌────────────────┐
│ 识别错误类型    │
└───────┬────────┘
        │
   ┌────┴────┬────────────┬────────────┬────────────┐
   │         │            │            │            │
   ▼         ▼            ▼            ▼            ▼
┌──────┐ ┌──────┐   ┌────────┐   ┌────────┐   ┌────────┐
│权限错误│ │网络错误│   │模型错误  │   │内存错误  │   │其他错误  │
└──┬───┘ └──┬───┘   └───┬────┘   └───┬────┘   └───┬────┘
   │        │           │            │            │
   ▼        ▼           ▼            ▼            ▼
┌─────────────────────────────────────────────────────────┐
│                      错误恢复策略                        │
│  ┌─────────────────────────────────────────────────────┐│
│  │ 权限错误: 显示引导页，引导用户开启权限               ││
│  │ 网络错误: 提示"离线模式可用"                        ││
│  │ 模型错误: 降级到文本搜索，提示重启应用               ││
│  │ 内存错误: 清理缓存，降低并发数                       ││
│  │ 其他错误: 显示通用错误提示，记录日志                 ││
│  └─────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
   │
   ▼
┌────────────────┐
│ 显示用户提示    │
└───────┬────────┘
        │
   ┌────┴────┐
   │         │
   ▼         ▼
┌──────┐ ┌──────┐
│重试按钮│ │忽略   │
└──────┘ └──────┘
```

### 6.3 常见错误处理示例

**权限错误处理**:
```typescript
try {
  await scannerService.startScan();
} catch (error) {
  if (error.code === 'SCAN_PERMISSION_DENIED') {
    // 显示权限引导页
    navigation.navigate('PermissionGuide', {
      permission: 'photos',
      onGranted: () => scannerService.startScan()
    });
  }
}
```

**模型错误处理**:
```typescript
try {
  const embedding = await aiService.encodeImage(uri);
} catch (error) {
  if (error.code === 'AI_OUT_OF_MEMORY') {
    // 清理缓存
    await cacheService.clear();
    // 提示用户
    showToast('内存不足，已清理缓存，请重试');
    // 降级：跳过 AI 索引，只保存元数据
    await dbService.insertPhoto({...photo, isIndexed: false});
  }
}
```

---

**文档版本**: v1.0  
**最后更新**: 2026-04-04  
**作者**: AI助手
